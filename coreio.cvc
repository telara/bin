#include "civic.h"
#include "core.h"

export int fibs(int n) {
    int fib3;
    for (int i = 1, n) {
        fib3 = fib(i);
        printNewlines(1);
        printInt(fib3);
        printNewlines(1);
    }
    return 0;
}

export int primes(int n) {
    for (int i = 2, n) {
        if (isprime(i) == true) {
            printInt(i);
            printNewlines(1);
        }
    }
    return 0;
}